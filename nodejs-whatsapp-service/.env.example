# WhatsApp API Service Configuration

# Port for WhatsApp API service (avoid conflict with Frappe)
WHATSAPP_API_PORT=3001

# Frappe server configuration
FRAPPE_HOST=localhost
FRAPPE_PORT=8002

# Engine: baileys (default) or wwebjs
WHATSAPP_ENGINE=baileys

# Optional: Override the complete webhook URL
# ERPNEXT_WEBHOOK=http://localhost:8002/api/method/whatsapp_integration.api.webhook.receive_message

# Session storage path (absolute or relative)
WA_SESSIONS_DIR=./sessions
